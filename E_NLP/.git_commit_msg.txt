E_NLP: fix Colab FileNotFoundError + add visual walkthrough docs for MHA, LayerNorm, and depth roadmap

Changes in this commit:

1. notebooks/05_self_attention.ipynb
   - Fixed FileNotFoundError: data_config.pkl / embedding_layer.pth not found on fresh Colab sessions
   - Replaced hard-coded file loading cell with self-contained LOAD OR GENERATE CONFIGURATION cell
   - On missing files: auto-downloads Shakespeare text, builds char-level vocab, saves both .pkl and .pth
   - Backward-compatible: still loads from files if previous notebooks were run

2. notebooks/00_llm_a_transformer_explained_With_example_for_Multihead.md (NEW)
   - Step-by-step visual walkthrough of Multi-Head Attention
   - Concrete numbers: "the cat sat", d_model=4, num_heads=2, head_size=2
   - Covers: why single head is insufficient, how x is NOT split (only W matrices differ),
     per-head Q/K/V computation, attention scores with causal mask, concatenation,
     output projection W_o, full ASCII architecture diagram, shape summary table,
     runnable MultiHeadAttention class, intuition summary

3. notebooks/00_llm_a_transformer_explained_With_example_for_LayerNorm.md (NEW)
   - Step-by-step visual walkthrough of Layer Normalization
   - Concrete numbers: token "cat" = [8.7, -12.4, 15.3, 9.1]
   - Covers: why normalize, BatchNorm vs LayerNorm distinction, formula (mean/var/normalize/scale+shift),
     full arithmetic walkthrough, all 3 tokens before/after, Pre-Norm vs Post-Norm pattern,
     gamma/beta learnable params, shape trace, runnable manual + PyTorch code

4. notebooks/TODO_next_suggested_path.md
   - Appended Q&A: "Is this the same architecture as ChatGPT/Claude?"
   - Added comparison table: your model vs GPT-4/Claude scale
   - Added "What You Already Know" checklist (complete GPT-2 stack)
   - Added 4-level depth roadmap:
       Level 1: BPE, GQA, RoPE, KV Cache
       Level 2: SFT, RLHF, mixed precision, gradient checkpointing
       Level 3: Flash Attention, MoE, Sliding Window, Cross-Attention
       Level 4: Quantization, Speculative Decoding, vLLM/PagedAttention
   - Added "Highest-Value Next 4 Topics" priority list
